import 'dotenv/config';

/**
 * Phase 0 stub — seed real data once schema lands in Phase 2.
 */
async function main() {
  console.log('seed: no tables defined yet; rerun after Phase 2 schema.');
}

main().catch((err) => {
  console.error(err);
  process.exit(1);
});
